<h2>Students and number of Professors</h2>
<% @students.each do |student| %>
  <section id="student-<%= student.id %>">
    <%= student.name %>: <%= student.professors.count %><br>
  </section>
<% end %>
